type Fruit = APPLE | BANANA of (Int)
type Foo = Int

let Fruit fruit = BANANA(3)
(* let Fruit fruit = APPLE *)
in let [Fruit] -> Int f =
  lambda (Fruit f) match (f) with
  | (_) -> -99
  | (APPLE) -> -1
  | (BANANA(3)) -> 100
  end

in print f fruit