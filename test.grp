type Fruit = APPLE | BANANA of (Int)
type Foo = Int

let Fruit fruit = BANANA(16)
(* let Fruit fruit = APPLE *)
in let [Fruit] -> Int f =
  lambda (Fruit g) match (g) with
  | (BANANA(x)) -> x
  end

in print f fruit