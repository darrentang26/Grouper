type Point = {x : Int, y : Int}
let [Int, Int] -> Int expon = lambda (Int base Int pwr)  if pwr == 1 
                                                         then base  
                                                         else base * (expon base (pwr - 1)) end end
                                                 let Int twoCubed = expon 2 3
in print twoCubed